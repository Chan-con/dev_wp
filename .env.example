# Ports
# ローカル開発では 8080 が無難です（ホストの 80 を空ける必要がないため）。
# EC2等で「IP直打ちで 80番」にしたい場合は `WP_HTTP_PORT=80` にして、
# `WP_SITE_URL` も `http://<public-ip>/` に合わせてください。
WP_HTTP_PORT=8080
PMA_HTTP_PORT=8081

# Images (optional; pin versions if you want)
# WP_IMAGE=wordpress:latest
# WPCLI_IMAGE=wordpress:cli

# Database
WP_DB_NAME=wordpress
WP_DB_USER=wp
WP_DB_PASSWORD=wp_password_change_me
WP_DB_ROOT_PASSWORD=root_password_change_me

# WordPress
WP_TABLE_PREFIX=wp_
WP_SITE_URL=http://localhost:8080
WP_SITE_TITLE=Dev WP
WP_ADMIN_USER=admin
WP_ADMIN_PASSWORD=admin_password_change_me
WP_ADMIN_EMAIL=admin@example.com
WP_LOCALE=ja
